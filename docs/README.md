# EasyX-RS 文档

这是 EasyX-RS 库的文档，托管在 GitHub Pages 上。

## 生成文档

要在本地生成文档，请使用以下命令：

```bash
cargo doc --no-deps --open
```

## 部署文档

文档会自动部署到 GitHub Pages，当代码推送到 `main` 分支时。

## 查看文档

- 在线文档：https://araraloren.github.io/easyx-rs/
- 本地文档：使用 `cargo doc --no-deps --open` 生成并打开